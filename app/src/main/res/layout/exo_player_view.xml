<?xml version="1.0" encoding="utf-8"?>
<!-- PlayerView's own layout, replaced by resource name the same way exo_player_control_view.xml
     is. It exists for one reason: exo_buffering is a plain wrap_content ProgressBar in media3's
     copy, PlayerView has no setter for it, and at its default size the spinner reads as smaller
     than the play button it replaces while a clip loads. Sized here to 64dp, just over the 56dp
     of exo_play_pause.

     Everything else is media3's structure, kept whole: PlayerView looks each child up by @id and
     turns the matching feature off when one is missing, so dropping a view here would silently
     disable it. Only the resources are ours, so that nothing private to the library is
     referenced. The spinner draws inside the content frame, below the controller. -->
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:override="true">

    <androidx.media3.ui.AspectRatioFrameLayout
        android:id="@id/exo_content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <!-- The video surface is inserted as the first child of the content frame. -->

        <View
            android:id="@id/exo_shutter"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black" />

        <ImageView
            android:id="@id/exo_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="fitXY" />

        <ImageView
            android:id="@id/exo_artwork"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="fitXY" />

        <androidx.media3.ui.SubtitleView
            android:id="@id/exo_subtitles"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <ProgressBar
            android:id="@id/exo_buffering"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:indeterminateTint="@color/player_control" />

        <TextView
            android:id="@id/exo_error_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="64dp"
            android:background="@drawable/player_error_background"
            android:fontFamily="@font/manrope"
            android:gravity="center"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:textColor="@color/player_control"
            android:textSize="14sp" />

    </androidx.media3.ui.AspectRatioFrameLayout>

    <FrameLayout
        android:id="@id/exo_ad_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <FrameLayout
        android:id="@id/exo_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <View
        android:id="@id/exo_controller_placeholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</merge>
